# Start the server
## 0. Clone the repo and submodule
```bash
git clone https://github.com/StarConnor/code-agent-redteam.git
cd code-agent-redteam
git submodule update --init --recursive
```

## 1. Create the environment
```bash
uv venv 
source .venv/bin/activate
uv sync
```

## 2. Build the docker image
see docker/README.md


## 3. Start the backend server
```bash
export V3_API_KEY="YOUR_API_KEY"
python -m src.queue_server
```

## 4.(Optional) Run the redteam task

```bash
export AGENT_BASE_URL="https://api.gpt.ge/v1"
export AGENT_API_KEY="sk-test123"
python -m src.queue_server
```

# Run experiment on existing server
```bash
cd test
python run_exp.py --config exp-dataset.yaml
python run_exp.py --config exp-sample.yaml
```

# 测试流程
### 构建测试的yaml文件
1. 选择测试模式：`mode`: `dataset`, `single_sample`。测试模式对应不同的端点（`/api/v1/coding-agent/tasks`, `/api/v1/coding-agent/tasks/single-sample`）
2. 指定后端url: `server_url`
3. 指定测试用户（方便区分）: `user`
4. 指定测试的agent对象: `{name}_{software}`: `cline_ide`, `cline_cli`
5. 指定是否覆盖曾经跑过的相同实验：`force_redo`
6. 指定攻击方法: `attack_method_name`。暂时可以设置为空字符串
7. 指定使用文件攻击时的文件内容：`file_attacks`,一个list
    - `name`: name
    - `content`: 恶意文件的内容
    - `container_path`：将这个文件放到容器中的文件地址
8. 指定虚拟机进行截图的时间间隔：`screenshot_time`, float
#### `single-sample` mode
1. （`single-sample`模式必选）指定sample：`sample`:
    - `id`: id
    - `input`: 给agent的user输入
    - `target`: 目标输出（暂时要设置为null）
    - `metadata`: 
        - `custom_html_content`: ipi_web攻击时设置的恶意网页
        - `auxiliary_files`： 需要给agent提供的辅助文件，如密钥等,可以指定文件在container里面的所有者（devuser）
2. 失败后是否不再尝试：`fail_immediately_on_error`, `true` or `false`
3. 给虚拟机里的ide上传settings：`ide_settings`:
    - `key`: `value`
    - 建议使用：
```yaml
ide_settings:
    remote.SSH.remotePlatform: 
      docker-container: "linux" 
    github.copilot.nextEditSuggestions.enabled: true
    github.copilot.chat.byok.ollamaEndpoint: "http://192.168.244.1:11434"
    extensions.autoCheckUpdates: false
    extensions.autoUpdate: false
    update.enableWindowsBackgroundUpdates: false
    update.mode: "none"
    update.showReleaseNotes: false
    extensions.ignoreRecommendations: true
```
#### `dataset` mode
1. 指定要筛选的数据集样本：`filter_dict`, 以`ipi_web_dataset`为例
    - `id`: list
        - "ipi_dl_cl_001"
        - "ipi_dl_cl_002"

### 测试日志和结果：
1. 日志：通过端点启动测试之后，使用websocket来连接并接收后端发送的**日志流**，参见`test/run_exp.py`文件中`listen_ws()`
2. 结果：同样websocket会在实验完成之后自动推送result，同样参见`test/run_exp.py`
3. 报告：在所有实验完成之后，调用`/api/v1/coding-agent/tasks/{task_id}/report`来获取报告